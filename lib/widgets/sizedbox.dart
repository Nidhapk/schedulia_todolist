import 'package:flutter/material.dart';

class ProfileSB extends StatelessWidget {
  const ProfileSB({super.key});

  @override
  Widget build(BuildContext context) {
    return  SizedBox(
      height: MediaQuery.of(context).size.height*0.05,
    );
  }
}

class PerfomanceSB extends StatelessWidget {
  const PerfomanceSB({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 15,
    );
  }
}
